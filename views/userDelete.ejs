<!DOCTYPE html>
<html lang="en">
  <% if (currentUser) { %>
    <head>
      <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Delete account</title>
      <link rel="stylesheet" href="/stylesheets/style.css" />
      <link rel="stylesheet" href="/stylesheets/userDeleteForm.css" />
    </head>
    <body>
      <%- include("navbar") %>
      <div class="pageContainer">
        <h1 class="title">Delete <%= currentUser.username %>'s account</h1>
        <p class="confirmText">Are you sure you want to delete your account?</p>
        <form id="deleteUser" action="/planner/user/<%= currentUser.id %>/delete" method="post">
            <button type="submit" class="deleteUserBtn">Delete account</button>
        </form>
        <a href="/planner/user/<%= currentUser.id %>/home" class="cancelBtn">Cancel</a>
      </div>
    </body>
  <% } else { %>
    <%- include("pageNotFound") %>
  <% } %>
</html>